// Optional runtime config for durable shared storage.
// Supabase anon keys are intended to be public, but keep service-role keys private.
window.DRIVE_AUDIT_SUPABASE_URL = window.DRIVE_AUDIT_SUPABASE_URL || '';
window.DRIVE_AUDIT_SUPABASE_ANON_KEY = window.DRIVE_AUDIT_SUPABASE_ANON_KEY || '';
